DropdownMenuSub from product-graph-editor. Use via `window.PrismDS.DropdownMenuSub` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface DropdownMenuSubProps {
  children?: React.ReactNode;
  open?: boolean;
  defaultOpen?: boolean;
}
```

## Related

`DropdownMenuSubContent`, `DropdownMenuSubTrigger`
