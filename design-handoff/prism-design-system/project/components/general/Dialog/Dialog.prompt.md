Dialog from product-graph-editor. Use via `window.PrismDS.Dialog` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface DialogProps {
  children?: React.ReactNode;
  open?: boolean;
  defaultOpen?: boolean;
  modal?: boolean;
}
```

## Related

`DialogClose`, `DialogContent`, `DialogDescription`, `DialogFooter`, `DialogHeader`, `DialogOverlay`, `DialogPortal`, `DialogTitle`, `DialogTrigger`
