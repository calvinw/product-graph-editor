MessageScrollerProvider from product-graph-editor. Use via `window.PrismDS.MessageScrollerProvider` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface MessageScrollerProviderProps {
  children?: React.ReactNode;
  autoScroll?: boolean;
  defaultScrollPosition?: "start" | "end" | "last-anchor";
  scrollEdgeThreshold?: number;
  scrollPreviousItemPeek?: number;
  scrollMargin?: number;
}
```
